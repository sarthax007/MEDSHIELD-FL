# ==============================================================================
# MedShield-FL — Development & Testing Dependencies
# ==============================================================================
# Install with:  pip install -r requirements-dev.txt
# (requirements.txt must already be installed)
#
# This file covers:
#   1. Testing — pytest ecosystem
#   2. Code quality — black, ruff, mypy
#   3. Pre-commit hooks
#   4. Notebook / exploration tools
#   5. Documentation helpers
# ==============================================================================

# Pull in all runtime deps first so a single install covers everything
-r requirements.txt

# ------------------------------------------------------------------------------
# 1. Testing — pytest ecosystem
# ------------------------------------------------------------------------------
pytest>=8.2.0,<9.0
pytest-asyncio>=0.23.0,<1.0   # async test support (FastAPI endpoints)
pytest-cov>=5.0.0,<6.0        # coverage reports
pytest-mock>=3.14.0,<4.0      # unittest.mock integration
httpx>=0.27.0,<1.0             # ASGI test client for FastAPI
anyio>=4.3.0,<5.0              # async runtime for tests

# ------------------------------------------------------------------------------
# 2. Code quality — formatters, linters, type checker
# ------------------------------------------------------------------------------
black>=24.4.0,<25.0            # opinionated Python formatter
ruff>=0.4.0,<1.0               # fast linter (replaces flake8 + isort + pyupgrade)
mypy>=1.10.0,<2.0              # static type checker
types-PyYAML>=6.0.12,<7.0     # mypy stubs for PyYAML
types-passlib>=1.7.7,<2.0     # mypy stubs for passlib

# ------------------------------------------------------------------------------
# 3. Pre-commit hooks
# ------------------------------------------------------------------------------
pre-commit>=3.7.0,<4.0

# ------------------------------------------------------------------------------
# 4. Notebook / exploration tools (optional but useful for data analysis tasks)
# ------------------------------------------------------------------------------
jupyter>=1.0.0,<2.0
ipykernel>=6.29.0,<7.0
ipywidgets>=8.1.0,<9.0

# ------------------------------------------------------------------------------
# 5. Documentation helpers
# ------------------------------------------------------------------------------
mkdocs>=1.6.0,<2.0
mkdocstrings[python]>=0.25.0,<1.0
